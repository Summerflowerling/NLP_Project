import {checkForContent } from "../client/js/contentCheck"

describe("Testing if user type in any text", () => {
    
    test("Testing the if textArea content is empty string ", () => {
        let input = "";
            
           expect(checkForContent).toBeTruthy();
})});